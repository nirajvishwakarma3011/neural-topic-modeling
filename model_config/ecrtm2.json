{
  "name": "ecrtm2",
  "params": {
    "k": 10,
    "vocab_size": 500,
    "device": "cuda",
    "verbose": true,

    "epochs": 150,
    "learning_rate": 0.002,
    "batch_size": 200,
    "lr_step_size": 125,
    "log_interval": 5,

    "topic_vec_dim": 256
  }
}